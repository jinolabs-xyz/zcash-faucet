{
  "name": "zcash-faucet-db-worker",
  "version": "0.1.0",
  "private": true,
  "description": "D1 proxy Worker for the Zcash faucet ledger.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema:local": "wrangler d1 execute zcash-faucet-db --local --file schema.sql",
    "schema:remote": "wrangler d1 execute zcash-faucet-db --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
